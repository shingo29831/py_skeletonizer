# --- コア依存ライブラリ (本番実行で必須) ---
# Git仕様に準拠した厳格な .gitignore ルール(ワイルドカード、否定パターンなど)を解析・フィルタリングするために使用
pathspec>=0.12.0

# --- 開発・テスト用ライブラリ (品質保証・保守用) ---
# 実装詳細ではなく「振る舞い」と「エラー」を自動テストするためのテストフレームワーク
pytest>=8.0.0

# コア構文の型安全性と「非推奨(Deprecated)」や技術的負債を静的解析で検知するリンター・フォーマッター
mypy>=1.8.0
ruff>=0.3.0